{
  "name": "circles",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client:build": "npm run build --workspace=client",
    "server:start": "npm run start --workspace=server"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "~5.6.2"
  }
}
